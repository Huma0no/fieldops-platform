# FieldOps Domain Contract Index

**Version:** 1.0
**Date:** 2026-08-17
**Status:** Current domain-contract reference

## Purpose and use

This index is the entity-level companion to `/docs/ARCHITECTURE-MAP.md`. It records current contracts, not schema fields, API details, UI inventory, or implementation status. Read it before changing behavior; follow the listed owning specification for detailed rules. `CONTRACT OPEN` means the behavior must not be invented during implementation.

| Entity | Purpose | Source of truth | Price? | Mutable when | Lifecycle / state | Major derived consumers | Owning spec | Important invariant |
|---|---|---|---|---|---|---|---|---|
| **Address** | Persistent physical-property anchor for visits. | `addresses` | No | Intake matching/creation; Dispatcher address edits. | No visit-like lifecycle. | History, properties, reports, weigh-in retrieval. | `shared/DATA_MODEL.md`; `dispatch/HISTORY-PROPERTIES-SPEC.md` | Address changes do not rewrite historical work records. |
| **Visit** | One operational service call. | `visits` plus its structured child source records. | Yes — resolved total. | Intake/review; assigned/in-progress Workspace before Generate Report; Dispatcher correction after completion. | `pending_review` → `in_lobby` → `assigned` → `in_progress` → `completed` / `temporarily` / `cancelled`. | Report, History, restock, refrigerant reporting, Ledger, Company outputs. | `shared/DATA_MODEL.md`; `shared/CORRECTIONS.md` | Structured visit data is the operational source of truth; no visit-level `closed`. |
| **Local Visit Draft** | Durable technician working copy of an assigned active visit. | Local FieldOps durable storage while active; reconciles with visit source data. | Carries the resolved local total; does not define an independent pricing rule. | Local-first during active work, before submission snapshot creation. | Active draft; no server terminal state by itself. | Workspace, local recovery, submission creation. | `OFFLINE-FIRST-CONTRACT.md` | Must survive navigation, app restart, and temporary offline operation; it is not a replay log of every mutation. |
| **Submission Snapshot** | Immutable local record created by Generate Report for safe delivery. | Durable local snapshot while pending; accepted structured visit source data after server ACK. | Carries the resolved total and persisted resolved values. | Immutable after creation; retry uses the same identity. | `pending` → `delivered`; failed/unknown attempts remain pending. | Delivery/retry, server finalization, derived Completion Report. | `OFFLINE-FIRST-CONTRACT.md`; `shared/DATA_MODEL.md` | A request attempt is not delivery; ACK is required and retries must not duplicate completion. |
| **Visit System / equipment context** | Per-visit HVAC system identity and resolved equipment context. | `visit_systems` with catalog-derived values persisted at creation/update. | No. | Intake/review and Workspace before Generate Report; Dispatcher correction. | No independent lifecycle. | Briefing, weigh-in, report, equipment/refrigerant reporting. | `shared/DATA_MODEL.md`; `workspace/WEIGHIN-SPEC.md` | Catalog edits do not rewrite historical system context. |
| **Service** | Base work service selected for a visit. | `visit_services`. | Yes — persisted resolved service price. | Assigned/in-progress before Generate Report. | Base service plus modifier fields; no independent status. | Pricing, Completion Report, Ledger. | `shared/CATALOG.md`; `workspace/SERVICE-TSTAT-SPEC.md` | **CONTRACT OPEN:** clearing the final selected base service has no canonical empty/clear semantics. |
| **Finish modifier** | Modifies a base service and report phrase. | `visit_services.is_finish`. | Modifier only — resolves AC/Heat service price; may trigger catalog item addon rules. | Assigned/in-progress before Generate Report. | Boolean modifier; not a base service row. | Pricing, Completion Report. | `shared/CATALOG.md`; `shared/REPORT-TEXT.md` | Never model Finish as an ordinary standalone service. |
| **Thermostat** | One catalog-backed model with visit quantity. | `visit_items` where category is `thermostat`. | **No.** | Intake/review and assigned/in-progress before Generate Report. | One selected model × quantity; no separate lifecycle. | Completion Report, restock/inventory. | `shared/CATALOG.md`; `workspace/SERVICE-TSTAT-SPEC.md` | Catalog-backed visit selection; no price contribution. |
| **Accessory** | Billable catalog work item. | `visit_items` where category is `accessory`. | Yes — persisted resolved item price. | Intake/review and assigned/in-progress before Generate Report. | Added/removed before finalization; no separate status. | Pricing, Completion Report, Restock when `tech_supplied`. | `shared/CATALOG.md`; `workspace/ACC-SPEC.md` | `tech_supplied` is catalog-derived; do not make the client authoritative for it. |
| **Fix** | Billable catalog repair item. | `visit_items` where category is `fix`. | Yes — persisted resolved item price. | Assigned/in-progress before Generate Report; Dispatcher correction after completion. | Added/removed before finalization; no separate status. | Pricing, Completion Report. | `shared/CATALOG.md`; `workspace/FIX-SPEC.md` | Fixes are not restockable under the current model. |
| **Other accessory** | Custom-price accessory with technician description. | `visit_items` row for catalog item `Other`, including `description`. | Yes — technician-entered persisted resolved price. | Assigned/in-progress before Generate Report; Dispatcher correction after completion. | Normal accessory lifecycle. | Pricing, Completion Report, Restock. | `shared/CATALOG.md`; `workspace/ACC-SPEC.md` | Requires nonblank description; remains normal `tech_supplied=true` accessory behavior. |
| **Other Fix** | Custom-price fix with technician description. | `visit_items` row for `Other Fix`, including `description`. | Yes — technician-entered persisted resolved price. | Assigned/in-progress before Generate Report; Dispatcher correction after completion. | Normal fix lifecycle. | Pricing, Completion Report. | `shared/CATALOG.md`; `workspace/FIX-SPEC.md` | Requires nonblank description; remains `tech_supplied=false`. |
| **Weight-In-Data billable item** | Billable catalog accessory, distinct from charge measurement capture. | `visit_items` row for Weight-In-Data. | Yes — persisted resolved item price. | Assigned/in-progress before Generate Report; Dispatcher correction after completion. | Normal accessory lifecycle. | Pricing, Completion Report, Restock. | `shared/CATALOG.md`; `workspace/ACC-SPEC.md` | Its resolved price is persisted; report formatting must not recreate Finish addon pricing. |
| **Weigh-In measurement record** | Per-system refrigerant and field-measurement capture. | `weigh_in_data`, associated with address/system context. | No. | Assigned/in-progress before Generate Report; Dispatcher correction after completion. | Per-system record; no independent status. | Address history, refrigerant report, Company form/output. | `workspace/WEIGHIN-SPEC.md`; `shared/DATA_MODEL.md` | Do not conflate this measurement domain with the billable Weight-In-Data item. |
| **Checklist Answer** | Optional field finding recorded as Yes/No. | Persisted visit checklist answers. | No. | Before Generate Report; Dispatcher correction after completion. | No independent lifecycle. | Completion Report; Cancel justification. | `workspace/CHECKLIST-NOTES-SPEC.md` | P-drain is a static inline reminder: no popup and no submission gate. |
| **Notes** | Technician observational text for a visit. | `visits.notes`. | No. | Before Generate Report; Dispatcher correction after completion. | No independent lifecycle. | Completion Report; Cancel justification; correction context. | `workspace/CHECKLIST-NOTES-SPEC.md` | Notes or one answered Checklist item is sufficient Cancel justification. |
| **Photo Evidence** | Visit photo associated with a defined evidence category/tag. | `visit_photos`. | No. | Before Generate Report; Dispatcher correction as allowed by source-data edit flow. | Stored evidence; completion bundles/upload process derives delivery state. | Completion bundle, Company form/output, History context. | `shared/API_CONTRACT.md`; `workspace/CHECKLIST-NOTES-SPEC.md` | Evidence is source data, not report text. |
| **Transfer** | Technician-to-technician reassignment handshake. | `transfers` plus the visit assignee. | No. | Current assignee initiates; recipient accepts/rejects before terminal completion. | `pending` / `accepted` / `rejected` / `expired`. | My Calls, Dispatch notification/log. | `fieldops/TRANSFER-SPEC.md`; `dispatch/TRANSFER-SPEC.md` | Transfer is not completion and does not create a visit status. |
| **Correction** | Post-completion request for Dispatcher source-data correction. | `corrections`. | No. | Technician requests only after terminal visit state while Ledger week is open; Dispatcher applies. | `open` / `applied` / `expired`. | Dispatcher workflow, notification/context; regenerated derivatives. | `shared/CORRECTIONS.md` | Technicians do not directly reopen finalized source data or edit report text. |
| **Catalog Item** | Master definition for thermostat, accessory, or fix selection and properties. | `catalog_items`. | Master default/configuration, not a visit price. | Dispatch administrative create/edit. | **CONTRACT OPEN:** no active/inactive lifecycle yet. | Intake, Workspace, server-side resolution, pricing, restock classification. | `shared/CATALOG.md`; `shared/DATA_MODEL.md` | Operational flows consume catalog; they never implicitly create/upsert it. |
| **Catalog Equipment** | Master equipment model/reference definition. | `catalog_equipment`. | No. | Dispatch administrative create/edit. | **CONTRACT OPEN:** no active/inactive lifecycle yet. | Intake, visit-system resolution, weigh-in, equipment/refrigerant reports. | `shared/CATALOG.md`; `shared/DATA_MODEL.md` | Historical visit context remains persisted when master data changes. |
| **Technician** | Active system actor, assignee, and compensation participant. | `technicians` and device authentication records. | No. | Dispatch roster administration; own permitted settings. | Active/deactivated; no deletion of historical identity. | Assignment, My Calls, Ledger, inventory/restock, audit context. | `shared/DATA_MODEL.md`; `dispatch/TECHNICIANS-SPEC.md` | Deactivation preserves history and does not silently reassign active visits. |
| **Restock / inventory consumption** | Current catalog-derived view of technician-supplied material and assigned stock. | `visit_items.tech_supplied`, finalized visits, `inventory_assignments`, `restock_records`. | Derived — not a visit price. | Dispatcher assigns inventory/marks restock; visit items change only before completion. | Restock records can be `restocked`; **CONTRACT OPEN:** consumption-level lifecycle is future. | Inventory, Restock report. | `dispatch/RESTOCK-INVENTORY-SPEC.md`; `shared/API_CONTRACT.md` | Do not describe the future per-consumption restock model as implemented. |
| **Pay Period / Compensation** | Financial aggregation and payout cycle. | `pay_periods`, lines, adjustments; derived from finalized visit totals. | Derived compensation, not visit price. | Dispatcher adjustments while open; financial actions by period state. | `open` / `closed` / `paid`. | Ledger and correction-window enforcement. | `dispatch/LEDGER-SPEC.md`; `shared/DATA_MODEL.md` | Financial closure is separate from visit completion. |

## Current contract boundaries

- **Multi-system Service:** **CONTRACT OPEN** — the per-system Service redesign is deferred; do not infer a future persistence model from current visit-level behavior.
- **OEM SC goal:** **CONTRACT OPEN** — current specification/code reconciliation remains unresolved; do not change measurement behavior until its canonical rule is established.
- **Catalog lifecycle:** **CONTRACT OPEN** — create/edit exists, but deactivate/reactivate and selector filtering are future work.
- **Restock consumption model:** **CONTRACT OPEN** — existing `tech_supplied` aggregation is current behavior, not the future consumption-state model.
- **Offline conflict and terminal evidence policy:** **CONTRACT OPEN** — offline reassignment/cancellation, token recovery, two-device work, stale catalog references, and whether evidence upload is required before terminal ACK are governed by `OFFLINE-FIRST-CONTRACT.md` and must not be inferred.

For implementation and QA status, use `docs/status/MASTER-FUNCTION-INDEX.md`; this index deliberately records contracts rather than status.
