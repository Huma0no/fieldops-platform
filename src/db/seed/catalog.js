// Not implemented — canonical catalog seed is scripts/seed-catalog.sql.
console.log('catalog seed: not yet implemented — see scripts/seed-catalog.sql for the canonical catalog seed');
process.exit(0);
